import { FloatingAnimationDirective } from './floating-animation.directive';

describe('FloatingAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new FloatingAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
