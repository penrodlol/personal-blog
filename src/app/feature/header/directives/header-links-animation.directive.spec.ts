import { HeaderLinksAnimationDirective } from './header-links-animation.directive';

describe('HeaderLinksAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderLinksAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
